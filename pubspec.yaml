name: cairn
description: "Cairn - a habit tracker where every completion is proven with an AI-verified photo."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.2
  drift: ">=2.26.0 <2.30.0"
  drift_flutter: ^0.2.7
  rrule: ^0.2.18
  uuid: ^4.5.3
  timezone: ^0.10.1
  sqlite3: ^2.9.0
  image_picker: ^1.2.2
  flutter_image_compress: ^2.4.0
  photo_manager: ^3.10.0
  path_provider: ^2.1.5
  connectivity_plus: ^7.2.0
  exif: ^3.3.0
  supabase_flutter: ^2.16.0
  intl: ^0.20.2
  camera: ^0.12.0+1
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  drift_dev: ">=2.26.0 <2.30.0"
  build_runner: ^2.4.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # Generates AppLocalizations from lib/l10n/*.arb via `flutter gen-l10n`
  # (also runs automatically before `flutter build`/`flutter run`). See
  # l10n.yaml for the generator configuration.
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # Bundled offline: the design files specify Zilla Slab (headings/task
  # titles) and Work Sans (everything else). Both are OFL-licensed, pulled
  # from the google/fonts GitHub repo as static TTFs where available. This
  # is an offline-first app, so these are bundled assets rather than
  # google_fonts' runtime CDN fetch: text must render correctly with no
  # network on first launch.
  #
  # Work Sans: google/fonts ships this family only as a single variable
  # font (`WorkSans[wght].ttf`, weight axis 100-900) now, not as separate
  # static per-weight TTFs. Declaring the same variable-font asset three
  # times below, once per weight, is Flutter's supported way to pin
  # specific static instances out of a variable font: each `weight:` entry
  # selects that instance from the font's weight axis at shaping time, no
  # runtime interpolation or network access involved. The design also
  # references Work Sans 450, which has no static instance in the family
  # (its only defined instances are the standard 100/200/.../900 steps);
  # every "450" usage in the designs is mapped to 500 throughout this app.
  fonts:
    - family: Zilla Slab
      fonts:
        - asset: assets/fonts/ZillaSlab-Regular.ttf
          weight: 400
        - asset: assets/fonts/ZillaSlab-Medium.ttf
          weight: 500
        - asset: assets/fonts/ZillaSlab-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/ZillaSlab-Bold.ttf
          weight: 700
    - family: Work Sans
      fonts:
        - asset: assets/fonts/WorkSans-Variable.ttf
          weight: 400
        - asset: assets/fonts/WorkSans-Variable.ttf
          weight: 500
        - asset: assets/fonts/WorkSans-Variable.ttf
          weight: 600
