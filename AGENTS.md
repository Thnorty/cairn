# Cairn architecture rules

AGENTS.md is the sole canonical instruction source for all human and AI contributors. Do not create vendor-specific mirrors, profiles, or alternate project instruction files.

Cairn is a Flutter habit-tracking app where every task completion is proven with an AI-verified photo. Users "climb" by earning metres per completion and progress through ranks.

## UI designs are canonical - do not invent UI

The `design/` folder contains the canonical UI designs as `.dc.html` files (Today/Home, camera capture & verify flow, Trail, New Habit variants, Stats, Profile, Premium, onboarding, empty states, failure states, daily limit). **All real screens must be implemented from those files** - palette, typography (Zilla Slab + Work Sans), pebble shapes, and exact copy come from them. No screen designs may be invented. The only exception is the Phase 1 debug screen, which is deliberately ugly/minimal and does not use the designs.

## Phase plan (plan of record: update this file when a phase completes or the plan changes)

Full rationale for every decision below lives in `docs/proof-todo-app-guideline.md`. That document is the source of truth; this is the sequencing. Phases may be re-planned: if you change them, rewrite this section so future sessions inherit the current map. Mark phases complete as you finish them, and record any decisions made along the way.

- **Phase 1: Data foundation. ✅ COMPLETE.** drift database, occurrence generation, streaks, metres/rank, repositories, `LocalDate`/`Clock` abstractions, minimal debug screen. 55 tests green. Not yet run on a physical device.

- **Phase 2: Proof pipeline.** Split so the agent-buildable core lands before anything that needs the human.
  - **2a: Client pipeline behind a `ProofVerifier` interface (no network, fully testable). ✅ COMPLETE** (156 tests green; smoke-tested on a physical Android 14 device: camera and gallery capture, compression, caps, offline/pending/retry, and recency all behave). Phase 1 audit fixes first (partial unique index so a tombstoned completion frees its slot; input validation in the repositories). Then: camera *and* gallery capture via `image_picker` (both always available for every task; there is no per-task restriction). Compress to ~150 KB for the wire. `photo_manager` recency pre-filter on the photo library's capture time. Verification attempts recorded in their own sync-ready table (a rejection is not a completion; the UNIQUE constraint on completions makes retries impossible otherwise). Enforce **5 successful proofs/day** and **3 attempts/task/day** (rejections do NOT burn the daily cap). Offline or verifier unavailable: `verification_status = 'pending'`, retry on reconnect/foreground; a pending completion counts toward streaks and caps optimistically, and is tombstoned (attempt recorded) if the retry comes back rejected. A configurable fake verifier drives tests and the debug screen.
    - Decisions made while building 2a: all proof policy lives in `ProofPolicy` (thresholds, caps, **and** the recency window), and every gate is enforced in `CompletionRepository.completeWithProof` rather than the UI, so no caller can bypass it. Guard order: back-fill, task exists, scheduled, duplicate, attempts cap, daily cap, recency, *then* the verifier. A stale photo never reaches the verifier and never writes an attempt row: **recency failures do not burn the attempts budget**, only verifier rejections do. An **unknown** photo timestamp fails *open* (`allowUnknownPhotoTime`, default true): `photo_manager` lookups legitimately return nothing on limited-permission or cloud-only libraries, and Gemini is the real gate. Recency window: 15 min. Photo lifecycle: verified and pending completions keep the file (a pending one needs it to retry), any rejection deletes it. `ProofFlowService` (capture, compress, save, complete) is the single entry point the Phase 3 UI calls; `ProofRetryTrigger` runs the pending retry on app foreground and on regaining connectivity.
    - Learned on device: `photo_manager`'s capture-time lookup alone is **not** enough on Android 13+, where `image_picker` goes through the system Photo Picker and hands the app a renamed copy in its cache, so the library match fails and every gallery pick fell open to "unknown". The resolver is therefore a chain: photo library first (its timestamp is hardest to forge), **EXIF `DateTimeOriginal` as the fallback** (the guideline's own nominated fallback). With that in place, stale gallery picks (photos and screenshots alike) are rejected and fresh ones pass, verified on device. Caps and attempt counts are exposed as repository methods (`successfulProofsToday`, `attemptsUsedToday`) because Phase 3's Daily Limit screen needs the same numbers.
  - **2b: Real verifier. ✅ COMPLETE.** Deployed (`verify-proof`, `verify_jwt` on) and verified end to end on a physical device: an honest photo passes, an unrelated photo is rejected with Gemini's own reason and burns an attempt, a screenshot verifies on its merits, and the Edge Function logs show 200s with no parser fallback (about 4s per round trip). Anonymous auth, `user_id` stamping + one-time backfill, and `SupabaseProofVerifier` are all live. The debug screen keeps `pass`/`reject`/`offline` fake modes alongside the default `real` mode, so device testing never has to burn Gemini calls. Anonymous Supabase auth on first launch, moved up from Phase 4: the Edge Function must verify a caller JWT and anon auth is what supplies it. Assign `user_id` from the first session so the Phase 4 upgrade preserves it. A **Supabase Edge Function** holds the Gemini API key (never in the app binary), verifies the caller's anon JWT, rate-limits (best-effort in-memory until Phase 4 tables exist), and returns structured JSON: `task_shown`, `confidence`, `is_screenshot_or_screen`, `reason`. Client-side mapping: `verified` iff `task_shown && confidence >= threshold` (default threshold 0.6, configurable). **`is_screenshot_or_screen` is recorded metadata only and never rejects on its own** (decided 2026-07-14): an earlier design had Gemini judge whether a screen was the *natural* evidence for the task, but that asks the model to guess which proof methods are legitimate and would reject honest ones nobody anticipated. The cost is a known, accepted bypass (screenshot an old photo and its capture time looks fresh, so recency passes); the guideline's own position is that the goal is friction, not a perfect lock, and a determined cheater can stage a shot regardless. Model: **Gemini 3.1 Flash-Lite**, minimal thinking. Every completion is AI-verified; there is no honor-system fallback.
  ⚠️ **Requires the human:** a real-device run of the 2a capture path (camera, gallery, `photo_manager` capture-time lookup and compression are all plugin-backed, so `flutter test` cannot exercise them; the debug screen's verifier-mode switch drives pass/reject/offline without a network). For 2b: Supabase project URL + anon key wired into the app config, Gemini API key, `supabase functions deploy` + secrets.

- **Phase 3: Real UI** from `design/`. ✅ COMPLETE. Every canonical `.dc.html` screen is implemented (Home/Today, camera capture + the full verify/pending/failed/too-old/no-retries outcome flow, Photo Review, Trail with per-task cairn history, New Habit variants, Stats, Profile, Premium + its three entry points, Daily Limit, empty/failure states, camera-unavailable, and the first-launch Onboarding flow gated by a local drift `AppSettings` flag). Two **just-in-time teaching** surfaces were added beyond the original screen list (decided 2026-07-19): a "Cairn Complete" celebration shown after Verify Result when a stone caps a cairn (reveals the +25 m cap bonus) and a "How cairns work" explainer opened from a "?" in the Trail header. The `design/` corpus had no surface for teaching the cairn logic or bonuses (bonuses are otherwise deliberately silent), so the orchestrator authored both designs (`design/Cairn Verify Result - Cairn Complete.dc.html`, `design/Cairn How Cairns Work.dc.html`) and the user approved them before implementation; they are now canonical alongside the rest. All screens are `flutter test` + screenshot verified; a physical-device pass on the Phase 3 UI is still outstanding (tracked in the orchestrator's memory).

- **Phase 4: Accounts + sync.** Split into an agent-buildable client engine and a human-gated remote half.
  - **4a: Client delta-sync engine (offline, behind a transport seam). ✅ COMPLETE.** 531 tests green. A `dirty` boolean column on the three syncable tables (`Tasks`/`Completions`/`VerificationAttempts`; schemaVersion 5 + migration), set `true` on every repository write and cleared only on push-ack. A `SyncTransport` seam (`lib/src/sync/sync_transport.dart`: `pull(cursor)` -> changed rows + new cursor, `push(batch)`) with a `FakeSyncTransport` for tests and an `UnimplementedError` `SupabaseSyncTransport` stub. `SyncService.syncOnce()` (`lib/src/sync/sync_service.dart`): pull-then-push, last-write-wins by `updatedAt` (pulled rows written `dirty=false`, applied directly to the tables bypassing the repos so their invariants/points logic and dirty=true never run), `deletedAt` tombstones are just another LWW field, cursor stored in the local `AppSettings` KV (`sync_pull_cursor`) and floored so it never moves backward; a `pull` throw aborts before touching anything, a `push` throw keeps the pull's effects but clears no `dirty`. The same-occurrence-different-id collision (two devices completing the same `(task,date,slot)`) is resolved on pull-apply by newer-`updatedAt`-wins, tombstoning the loser **before** upserting the winner so the partial unique index holds. `syncServiceProvider`/`syncTransportProvider` exist but are **not** watched/triggered in the app lifecycle yet.
  - **4b: Remote half. Record sync ✅ DONE (2026-07-21); push AND pull paths both device-verified on physical devices (push 2026-07-21, pull two-install 2026-07-23).** The remote tables (`public.tasks`/`completions`/`verification_attempts`, snake_case, mirror the drift schema minus the client-only `dirty`), per-user **RLS** (`user_id = auth.uid()`, SELECT/INSERT/UPDATE only, no DELETE), and a server-side last-write-wins `before update` trigger (`sync_lww_guard`, keeps the newer `updated_at`) are applied on project `cuerjdnznhsdgqbajgvv` (migrations `20260721134845_create_sync_tables`, `20260721135806_sync_lww_guard`, and `20260721141500_revoke_rls_auto_enable_public_execute` (tidies the pre-existing `ensure_rls` helper's ACL down to `postgres`/`service_role`, dropping the unexploitable default anon/authenticated/PUBLIC EXECUTE grant); applied via the Supabase MCP because `supabase db push` hangs on this network's direct-DB connection, with matching local files under `supabase/migrations/`). The real `SupabaseSyncTransport` (`lib/src/sync/supabase_sync_transport.dart`: `pull` = `select updated_at > cursor` per table under RLS -> drift rows with `dirty=false`; `push` = upsert dirty rows with `user_id` stamped and `dirty` dropped, behind a mockable `SyncPostgrest` seam), and a guarded `SyncTrigger` (`lib/src/sync/sync_trigger.dart`, runs `syncOnce()` on foreground/reconnect only when configured AND signed-in; watched in `CairnApp`) are built and unit-tested (556 tests green). `syncTransportProvider` returns the real transport when `AppConfig.isConfigured`, else `UnconfiguredSyncTransport`. The on-device **pull**-path / two-install end-to-end verification is now ✅ DONE (2026-07-23): a second physical-device install signed into the same email/password account and the first device's whole trail (tasks, completions, stones, rank) pulled down, with the Keep-which-trail chooser correctly NOT shown (local was empty, so onboarding sign-in adopts the account outright). The **push** half was device-verified 2026-07-21 (a real install pushed the migration-swept backlog of 5 tasks / 5 completions / 1 attempt plus a freshly-created habit up to the RLS tables, all stamped with the anon `user_id`, on a plain background->foreground). STILL OPEN in 4b: the **email/password account upgrade** preserving the same `user_id` (BUILT and code/test/screenshot-verified 2026-07-22; substantially device-verified 2026-07-23 - create, OTP verify, sign-in, sign-out, the replace chooser, and pull all worked on device - EXCEPT the forgot-password/reset flow and the 2026-07-23 account-UI polish items, which still need a walk on the freshly-installed profile APK; see the dedicated Account-upgrade sub-bullet below); **durable server-side rate limiting + the global Edge Function ceiling ✅ DONE (deployed 2026-07-23).** A `verify_quota_counters` table + `SECURITY DEFINER` `check_and_bump_verify_quota(user_cap, global_cap)` (migration `20260723190123_verify_rate_limits`, applied via MCP; local file under `supabase/migrations/`) enforce a durable per-user-per-UTC-day cap AND a global-per-UTC-day ceiling; the RPC runs with the caller's JWT so `verify-proof` still holds no service-role access. `verify-proof` (redeployed v3) now runs in-memory burst (`VERIFY_BURST_PER_MIN`=20) -> durable RPC (`VERIFY_USER_DAILY_CAP`=30, `VERIFY_GLOBAL_DAILY_CAP`=10000), failing OPEN on a DB error; all three are tunable via function env secrets, documented in the committed non-sensitive `supabase/functions/.env`. No client change was needed: the client already maps the resulting 429 to VerifierUnavailable -> pending/retry. Accepted/expected security-advisor notices from this: `verify_quota_counters` "RLS enabled, no policy" (deliberate - only the definer function touches it) and the definer function being anon/authenticated-executable (required so the caller's JWT can invoke it; the direct-RPC exposure is bounded to ~`user_cap` per identity, the same identity-farming bound captcha will later tighten). STILL deferred: **captcha on anonymous sign-ins** (the remaining root-cause fix for identity farming; needs a 3rd-party provider + Flutter widget + dashboard config) and the **server-side no-back-fill guard** ("today" is a client-local date, needs a timezone story). Also newly flagged for pre-release: **Supabase Auth leaked-password protection is OFF** - a one-click dashboard enable, worthwhile now that email/password accounts exist. No PowerSync, no realtime. **Record sync is free**; cloud photo backup is premium and post-MVP.
  - **Account upgrade (email/password). ✅ BUILT 2026-07-22 (WO-A logic core + WO-B UI), 659 tests green + screenshot-verified; ⚠️ pending a physical-device pass and the human's Supabase auth-settings.** Lets a user turn the anonymous session into a real account preserving the same `user_id`, sign into an existing account, reset a forgotten password, and sign out. Decisions: (1) email/reset proof is a **6-digit OTP entered in-app, NOT an email deep-link** (zero deep-link config, works even if the mail is opened on another device, one shared code screen for both flows); (2) anonymous->permanent is `updateEmail` -> `verifyOtp(emailChange)` -> `updatePassword`, so the **email is verified BEFORE the password is set** (Supabase's required order, which is exactly what enforces "no active account until the email is verified"); (3) **forgot-password is in scope** via the same OTP screen + a set-new-password screen (`resetPasswordForEmail` -> `verifyOtp(recovery)` -> `updatePassword`); (4) **sign-out keeps ALL local data and reverts to a fresh anonymous session** (never wipes local); (5) signing into an existing account on a device that already has any live task OR completion shows a **Keep which trail** chooser that **REPLACES, never merges** - "keep this device" tombstones the account's rows and pushes local up (RLS has no DELETE, so removal is by tombstone), "use the account" hard-wipes local then pulls; onboarding sign-in (empty local) skips the chooser and adopts the account. Architecture: `GoTrueGateway` seam + typed `AccountError` mapping + expanded `AuthService`; `AccountService` orchestrator (`SignInComplete`/`SignInNeedsTrailChoice`); `SyncService.replaceLocalWithCloud`/`replaceCloudWithLocal` + `remoteTrailSummary`, `CompletionRepository.localTrailSummary`, `TaskRepository.hasAnyLiveTasks`; UI in `lib/src/ui/account/*` (create / sign-in / enter-code[dual purpose] / set-new-password / keep-which-trail, reusable `EmailField`/`PasswordField`/`OtpCodeInput`, a sage `PrimaryButton` variant, Profile row reactive via `accountStateProvider`, onboarding "Sign in" wired). Canonical design: `design/Cairn Account.dc.html`. This also **unblocks the on-device pull-path verification** (a second install can now sign into the same account to share identity). ⚠️ Before the device pass the human must set this project's Supabase auth: **Confirm email ON**, the real **password policy** (confirm `kMinPasswordLength` in `account_policy.dart`, currently 6), and put the **6-digit `{{ .Token }}` in the confirm/change/reset email templates**.
  ⚠️ **Requires the human:** Supabase project config, RLS review.

- **Release prep (Android-first, personal project). 🚧 IN PROGRESS.** Concrete pre-release engineering blockers, knocked out 2026-07-24: production **application id + namespace `com.thnorty.cairn`** (was `com.example.cairn`; `MainActivity.kt` moved to the new package); **release signing** wired from a gitignored `android/key.properties` (falls back to debug when absent; `android/key.properties.example` is the committed template); **INTERNET permission moved into the main manifest** so release builds get network (Flutter only auto-adds it to debug/profile); launcher label `Cairn`; real pubspec description; and a **branded adaptive launcher icon** (`design/Cairn App Icon.dc.html`: charcoal `#33302B` stacked river-stones on parchment `#DDD4C3`) shipped as vector adaptive drawables (foreground + `@color/ic_launcher_background` + a **monochrome layer** for Android 13+ Material You themed icons, wired via `mipmap-anydpi-v26/ic_launcher.xml`, cairn scaled 0.86 into the 66dp safe zone) plus regenerated branded legacy `mipmap-*/ic_launcher.png` fallbacks for < API 26. Release APK builds green. ⚠️ **Requires the human:** generate the upload keystore (`keytool` command in `key.properties.example`) + create `android/key.properties`; flip on Supabase leaked-password protection; author + host a Privacy Policy + Terms.   - **Branded splash screen ✅ DONE (2026-08-06).** Replaces Flutter's untouched template splash (plain white on light, `?android:colorBackground` on dark). The charcoal `#33302B` cairn centred on parchment, **icon only, no wordmark** - deliberately, because **Android 12+ forces its own splash API which accepts only an icon on a solid background**, so icon-only is the one treatment identical on every version. Background is `#E9E1D3` = `AppColors.screenBackground`, NOT the launcher icon's `#DDD4C3`, so there is no colour jump when Flutter draws its first frame (verified on device: the app renders `(232,224,210)` at handoff). New `drawable/splash_cairn.xml` (paths copied from `ic_launcher_monochrome.xml`, re-filled charcoal, 0.86 scale) + `values/colors.xml` + `values-v31/styles.xml` for the 12+ platform API + the pre-31 `launch_background.xml` layer-lists. Sizing: `windowSplashScreenAnimatedIcon` is a 288dp canvas whose **outer third is masked off** (only the inner 192dp shows); at 0.86 the cairn's farthest point sits ~32.4 of the 36 available units, so it clears the mask. **`values-night/styles.xml` was DELETED, and must not come back:** in Android's resource-qualifier precedence **night mode ranks ABOVE platform version**, so a `values-night` styles.xml shadows `values-v31` entirely and a dark-mode Android 12+ device would silently fall back to the OS default splash. With no night override, dark mode falls through to `values/` below API 31 and to `values-v31/` at 31+, which is correct in both cases. (An initial `values-night` file "matched day"; that is exactly the trap, and independent review caught it.)
  - **Splash force-dark opt-out ✅ DONE (2026-08-06).** With system dark mode on, the splash rendered muddy: measured on device the parchment fell from `#E9E1D3` (233,225,211) to (159,155,146) AND the charcoal cairn ROSE from `#33302B` (51,48,43) to (75,72,67). Contrast squeezed toward grey from **both** directions is force-dark's signature, not a wrong colour resource. Only the splash was affected because force-dark operates at the View layer and the splash is a window-background drawable; Flutter renders to its own surface it cannot reach, which is why the app itself was always correct and the seam showed only at launch. Fixed with `android:forceDarkAllowed="false"` on both `LaunchTheme` and `NormalTheme`, in `values/` and `values-v31/` (ignored below API 29, so it can live in `values/`). **Verified on a Galaxy S23 / Android 14 in dark mode: background (233,225,211) and cairn (51,48,43), both pixel-exact.** ⚠️ **MIUI ignores this opt-out**: Xiaomi's "Dark mode for all apps" is a vendor feature that overrides the AOSP opt-out, so the splash is still darkened there. Judged acceptable and NOT worked around - it is a device-level setting the user switched on which darkens every app, and it only affects a sub-second screen. If it is ever worth fighting, the known technique is to ship the splash as a **bitmap** (force-dark generally leaves bitmaps alone) rather than a colour + vector.
  - **Debug-screen release gate ✅ DONE (2026-08-06).** The Phase 1 debug screen was reachable in **release** builds by long-pressing the wordmark on Today, which handed any user a `FakeProofVerifier` in `pass` mode (every photo verifies, no Gemini call - the app's whole premise off) and a premium-entitlement override (lifting the daily proof cap, the only live paid benefit). `AppShell` now passes `onOpenDebug` only under `kDebugMode`, which also lets the tree-shaker drop `DebugScreen` from the binary (release APK 68.1 -> 66.7 MB).
  - **Privacy links ✅ DONE (2026-08-06).** Profile > Settings > Privacy was `onTap: _noOp` and onboarding's "Learn more about privacy" showed a coming-soon snackbar, both long after `AppConfig.privacyUrl` was filled in at 5c. Both now open it via a shared `launchExternalUrl` (`lib/src/ui/external_url.dart`), extracted so the Premium footer's links, Profile and onboarding cannot drift; a duplicate private copy inside `premium_screen.dart`'s `_FooterLinks` was removed at the same time. `url_launcher_platform_interface` is now a direct dev dependency so `test/support/fake_url_launcher.dart` can assert WHICH url opens, pinning all of them to `AppConfig.privacyUrl`.
  - **In-app account deletion ✅ DONE (2026-08-06, implemented in two work orders). Deployed AND device-verified end to end.** `delete-account` is deployed (v1, ACTIVE, `verify_jwt` on) and no function secret was needed: Supabase auto-injects `SUPABASE_URL`/`SUPABASE_ANON_KEY`/`SUPABASE_SERVICE_ROLE_KEY` into every Edge Function. Verified on a physical device with a real throwaway account: **before** = 2 tasks / 1 completion / 1 attempt / 1 quota row under the email account; **after** = 0 of everything AND the `auth.users` row gone by both id and email (so the address is reusable and nothing was orphaned despite there being no cascade). Critically, a **new anonymous user created seconds later owned exactly those same counts (2/1/1)**, which is the on-hardware proof that `clearUserId()` works: without it those rows would still carry the dead account's `user_id`, `backfillUserId` would skip them (it only claims `user_id IS NULL`), the new anon user would own ZERO rows, the trail would still display locally, and the loss would only surface when the user next made an account. On-device UI also confirmed: the destructive red-clay confirm button, the trail still present on Today afterwards, and Profile reverting to "Climbing anonymously". A hard Play requirement. Canonical design: `design/Cairn Account - Delete.dc.html` (authored by the orchestrator, user-approved before implementation). Decisions: (1) offered **only when signed in with a real email account** - an anonymous user has no "account" in the user-facing sense; (2) it deletes the remote account and all cloud rows but **KEEPS all local data** - Cairn is local-first, the cloud is backup/portability, so delete-account is simply sign-out plus destroying the remote identity, and the design's sage "your trail stays on this phone" card is the single most important thing on the screen; (3) **immediate and irreversible**, no grace period (that would need a scheduled purge job that does not exist); (4) gated on **re-entering the password** and then a `CairnDialog` confirm - the password proves who, the dialog proves intent; (5) afterwards the app reverts to a fresh anonymous session and **anonymous sync resumes**, so the trail re-uploads under a new anon id within seconds. Because of (5) the clay card deliberately names what is destroyed ("your account and the email on it, along with everything backed up under it") rather than promising an ongoing absence from the server, which would stop being true immediately.
    - **Client half:** `AccountDeleter` seam + `SupabaseAccountDeleter` (invokes the `delete-account` function) + `UnconfiguredAccountDeleter` + a fake; `AccountService.deleteAccount(password)` in a strict order - re-auth, best-effort `premium.logOut()`, remote delete (**throws => abort before ANY local change, so a retry still works**), release local ownership, sign out; `DeleteAccountScreen` + a destructive Profile settings row gated on a signed-in email.
    - **`clearUserId()` is load-bearing and non-obvious.** Deleting the account leaves local rows stamped with the dead account's `user_id`, and `backfillUserId` only claims rows `where user_id IS NULL`. Without nulling it the trail still *displays* (no local read filters on `user_id`) but a LATER new account would never claim or back it up - silent, invisible loss at the next backup. A test asserts the NULL explicitly. `SupabaseSyncTransport.push` already stamps the caller's id "even for a row whose local `user_id` was null", so released rows re-sync correctly.
    - **Server half:** `supabase/functions/delete-account/index.ts` + `verify_jwt = true` in `config.toml`. The caller id comes **only from the JWT** via a forwarded-Authorization anon client calling `auth.getUser()` - it parses no request body at all, deliberately: a user-id parameter would let any authenticated user delete anyone. It is the FIRST Cairn function to hold `SUPABASE_SERVICE_ROLE_KEY` (`verify-proof` deliberately holds none), needed because deleting an `auth.users` row requires the admin API and RLS grants the client no DELETE; the service-role client is built only after identity is resolved and every statement is scoped to that one id. **There is no `ON DELETE CASCADE`** on the sync tables (`user_id` is a plain column with no FK to `auth.users`), so rows are deleted explicitly, and **rows go before the auth user** - the reverse order would leave rows owned by an identity that can no longer authenticate, so nothing could ever clean them up. Returns 200 only if every step succeeded.
    - **Independent review caught two defects despite a green report**, neither visible to `analyze`/`test`/build: it gave the delete dialog a scarier button by changing what the **shared** `CairnDialogTone.clay` renders, which silently repainted the **sign-out** dialog destructive too (fixed by adding a separate `CairnDialogTone.destructive`; 3 regression tests now pin each tone's confirm gradient, which nothing did before); and the Profile row pushed with `rootNavigator: true` but popped without it, working only by luck because nothing nests a Navigator around `AppShell`.
    - **Sensitive work-order note:** pass work involving credentials, privileged APIs, or deletion by file reference instead of inline command text. Inline security-sensitive text can be blocked before execution, while a short instruction to read a scoped work-order file avoids that failure mode.
  ⚠️ **Requires the human:** the Play Data Safety form must declare what is actually deleted (account + all synced cloud data; on-device data stays on device). The user decided 2026-08-06 that a **web-accessible account-deletion URL is out of scope**, having been told Play's Data Safety form generally requires one once account creation is declared; flagged once, their call, do not re-raise.
  STILL OPEN: store-listing assets.

- **Phase 5: Monetization (RevenueCat subscriptions). 🚧 IN PROGRESS.** Premium is sold as a **subscription (monthly + annual, 7-day free trial)** via **RevenueCat** (`purchases_flutter`), decided 2026-07-24. Record sync stays free; cloud photo backup stays premium/post-MVP. The canonical paywall is `design/Cairn Premium.dc.html` (monthly `$2.99`, yearly `$20.99` "save 41%", 7-day trial CTA; prices set 2026-07-24, revised to these figures 2026-07-25). It advertises 5 benefits but only ONE is buildable today - **"Unlimited AI proofs"** = lifting the `ProofPolicy.dailyCap` of 5, cleanly enforced in `CompletionRepository` and wired via `proofPolicyProvider`. The other four (cloud photo backup, deeper insights, home-screen widgets, stone styles) are unbuilt roadmap; decision (2026-07-24): keep the design's rows but tag the four unbuilt ones **"Coming soon"** so the paywall is honest and Play-review-safe (the one live benefit leads). Structure mirrors the Phase 4 sync split (seam + fake + inert-unconfigured impl first, real network impl + human external setup later):
  - **5a: Entitlement engine behind a seam (agent-buildable, fully testable). ✅ DONE (2026-07-24, 715 tests green, analyze clean).** A `PremiumService` interface (`lib/src/premium/`) mirroring the `SyncTransport` pattern: reactive `isPremiumStream`, `loadOfferings()`, `purchase(plan)`, `restore()`, `logIn(userId)`/`logOut()`, with app-level DTOs (`PremiumPlan`/`PremiumOffering`/sealed `PremiumPurchaseOutcome`) so RevenueCat types never leak past the seam. An inert `UnconfiguredPremiumService` (always non-premium; used until `AppConfig.isPremiumConfigured`, i.e. `REVENUECAT_API_KEY` set) and a `test/support/fake_premium_service.dart`. Providers: `premiumServiceProvider`, a private status `StreamProvider`, a `debugPremiumOverrideProvider` (StateProvider<bool?>, mirrors `debugVerifierModeProvider`) so the gate is device-testable with no billing backend, and `premiumStatusProvider` (the one reactive source of truth). The one live gate: make `proofPolicyProvider` premium-aware so premium lifts the daily cap (represent unlimited as a **nullable `dailyCap`**, threaded through the repo guards / `StatsService` / Daily-Limit + debug UI). Implemented in two steps (5a-1 seam+fake+providers; 5a-2 the gate).
  - **5b: Real RevenueCat impl + wire the purchase UI (agent-buildable). Split into 5b-1 (service) + 5b-2 (paywall UI).**
    - **5b-1: real service + startup + identity hook. ✅ DONE (2026-07-24, 715 tests green, analyze clean, `flutter build apk --debug` green so the native Android merge is verified).** `purchases_flutter: ^10.4.3` added; `lib/src/premium/revenuecat_premium_service.dart` (`RevenueCatPremiumService implements PremiumService`, entitlement id `premium`, `addCustomerInfoUpdateListener` -> a race-free per-listen replay `isPremiumStream` mirroring the fake's structure, `getOfferings` -> `PremiumOffering` keeping a private `id -> Package` map for purchase, `Purchases.purchase(PurchaseParams.package(pkg))` [the v10 API] then a fresh `getCustomerInfo()` for the entitlement so it doesn't depend on the purchase return type, `restorePurchases`/`logIn`/`logOut`, user-cancel mapped via `e is PlatformException && PurchasesErrorHelper.getErrorCode(e) == purchaseCancelledError` -> `PremiumPurchaseCancelled`); `premiumServiceProvider` now returns it when `AppConfig.isPremiumConfigured` else `UnconfiguredPremiumService` (mirrors `syncTransportProvider`); `main.dart` calls `Purchases.configure(PurchasesConfiguration(AppConfig.revenueCatApiKey))` in a swallowing try/catch only when configured; `authBootstrapProvider` associates the RC identity with the stable Supabase `user_id` via `logIn(userId)` (no-op on the unconfigured service). Fully inert at runtime until 5c supplies the key. `logOut`-on-sign-out is deferred to 5b-2.
    - **5b-2: wire the Premium paywall UI. ✅ DONE (2026-07-24, 719 tests green, analyze clean, `flutter build apk --debug` green for the `url_launcher` native merge).** Because the canonical `design/Cairn Premium.dc.html` had no billing states at all, the orchestrator first **authored the designs and the user approved them** (same route as the Phase 3 "Cairn Complete" / "How cairns work" surfaces): the value list in `Cairn Premium.dc.html` gained a sage **"Available now"** tag on the one live benefit and a muted **"Coming soon"** tag + dimmed content on the four unbuilt rows, and a new companion file **`design/Cairn Premium - Billing States.dc.html`** is now canonical for the purchasing / success / error / restore / already-premium states. Implementation: `PremiumScreen` is a `ConsumerStatefulWidget` reading real store prices from a new `premiumOfferingProvider` (`FutureProvider<PremiumOffering?>`, falling back to the hardcoded design prices while loading/unconfigured so it never renders blank); plan select -> `purchase()` with an in-place spinner on the CTA (`PrimaryButton`'s existing `onPressed: null` + `icon` slot, no forked widget) and non-interactive plan cards while in flight; success -> sage snackbar + pop (entitlement flips reactively via `premiumStatusProvider`, never a manual flag); **cancel is silent**; failure -> neutral snackbar. Restore reports both outcomes. When already entitled, the plan cards + trial CTA are replaced by the sage entitlement panel + a "Manage subscription" button (opens the OS store-subscriptions page via a new `url_launcher` dep). `MessageSnackBar` gained an optional `MessageTone {neutral, success, muted}` where `neutral` keeps today's exact look, so no existing call site changed. Profile's Premium row reflects entitlement and its "Restore purchase" row is live. **Terms/Privacy links stay visible but inert** behind new empty-by-default `AppConfig.termsUrl`/`privacyUrl` dart-defines: no invented placeholder URLs, filled in at 5c. `AccountService.signOut()` releases the billing identity first (`premium?.logOut()`), wrapped best-effort so a billing failure can never strand a user in a session they asked to leave.
    - **5b-3: an account is REQUIRED to subscribe or restore. ✅ DONE (2026-08-06, 937 tests green, analyze clean).** Decided with the user: buying while signed out was possible and should not be. RevenueCat identifies the buyer by Cairn's own `user_id`, and an anonymous id is **not durable** - signing out or deleting the account mints a NEW anonymous id, so the entitlement stops following the user; Play still knows about the purchase but recovery depends on the same Google account plus a manual Restore. Canonical design: **`design/Cairn Premium - Account Required.dc.html`**, which is deliberately `Cairn Premium.dc.html` **verbatim with exactly two deltas** (the sage "why an account" card above the footer, and the footer CTA), so the signed-out state cannot drift from the offer. **The gate is on the button, not the door:** a signed-out user still sees the crest, headline, all five value rows AND both plan cards before anything is asked of them - hiding the offer would also leave Stats' locked insights card and the Daily Limit screen with nowhere to send anyone. Implemented as a THIRD condition on the composition `PremiumScreen` already used for its already-premium state (a conditional `_AccountRequiredCard` + a varying `_Footer`), explicitly **not** a second screen or route: all five entry points still call `openPremiumScreen`. The signed-out CTA is `PrimaryButtonColor.sage` rather than terracotta because terracotta is the buy button everywhere else and this one opens the account flow instead of buying. **Restore is gated too** (user decision 2026-08-06, against the orchestrator's recommendation - the tradeoff is that an existing anonymous buyer must make an account before recovering, which is inconvenient but not a dead end); because the restore links are small and contextless, tapping one while signed out shows a sage `CairnDialog` first rather than silently presenting a signup form. Gated on **both** the paywall footer and Profile's settings row. No `onComplete` is passed to `AccountFlow`: `_completeAndClose` already invalidates `accountStateProvider` and closes itself, so popping reveals the paywall which rebuilds with the real purchase CTA. Review also added **`AccountState.isSignedIn`** and routed every caller through it - the predicate had been hand-written at four sites, and `!isAnonymous` alone is NOT sufficient (it is also false when there is no session at all), so a fifth careless copy would have treated a not-yet-initialised session as signed in. ⚠️ **Not device-tested end to end:** the return-trip navigation is covered by a widget test driving the real `AccountFlow`, but the composition of *real* Supabase account creation with the return to the paywall has not been walked on hardware. ⚠️ **Check before real money moves:** RevenueCat's **transfer-purchases** setting - restore now always runs under a different app-user-id than an anonymous purchase used, and if purchases are set to stay with the original ID, restore-after-signup will silently not grant the entitlement.
  - **5c: ⚠️ Human-gated external setup (agent cannot do).** Google Play developer account + upload the app + create the monthly/annual subscription products with the 7-day trial intro offer; a RevenueCat account + connect Play + define the `premium` entitlement/offering; the public RC Android SDK key into config (`--dart-define REVENUECAT_API_KEY=` or bake the default); hosted Privacy Policy + Terms URLs.
  ⚠️ **Requires the human:** Play Console + RevenueCat dashboards, product/entitlement setup, the public SDK key, legal URLs.

- **Phase 6: Notifications. ✅ CODE COMPLETE (2026-08-06, 910 tests green, analyze clean, debug AND release APKs both build).** Local habit reminders, no push service, no server. Decided with the user 2026-08-05: **per-occurrence reminders + streak-at-risk warnings only** (no end-of-day "you have N unproven habits" digest - considered and cut as the one most likely to feel like nagging), **a tap goes straight to the camera** for that exact occurrence, the default reminder time is **09:00 and changeable in settings**, onboarding gains **a skippable permission screen after the camera ask**, and there is an **in-app notifications toggle**. Canonical designs authored by the orchestrator and approved by the user before implementation (same route as the Phase 3/5b-2 surfaces): `design/Cairn Notifications.dc.html` (3 states: on / off / blocked-by-Android) and `design/Cairn Onboarding Notifications.dc.html` (step 5 of 5).
  - **6a: Scheduling engine behind a seam (no plugin, fully testable). ✅ DONE.** `NotificationPlanner` (`lib/src/services/notification_planner.dart`) computes the plan purely from the repositories + `Clock`: a **14-day rolling window** re-planned on foreground and on any `tasks`/`completions` write, never reminding for an already-completed occurrence, never scheduling in the past, and suppressing a streak warning when that task already has a reminder due later the same evening. Streak warnings fire at a fixed **20:00** local, at most one per task per day, only when the task's current streak (not counting today, which is still open) is >= 1 and today still has an incomplete slot. `NotificationScheduler` seam + `UnconfiguredNotificationScheduler` + `FakeNotificationScheduler`; `PendingNotification`/`NotificationPayload` with a **stable FNV-1a id** per (task, date, slot, kind) so a re-plan replaces rather than duplicates (Dart's own `hashCode` is not stable across restarts, which this needs); `NotificationTrigger` wired into the app lifecycle. Three new `AppSettings` keys (`reminders_enabled` default OFF, `default_reminder_time` default `09:00`, `streak_warnings_enabled` default ON once reminders are on).
  - **6b: Real plugin, permission, UI and tap routing. ✅ DONE.** `flutter_local_notifications: ^20.1.0` + `flutter_timezone: ^5.1.0`; `LocalNotificationsScheduler` (`lib/src/notifications/local_notifications_scheduler.dart`) with **two Android channels** (`cairn_reminders` / `cairn_streak_warnings`, so silencing one at the OS level does not silence the other - which is why `PendingNotification` carries its `kind` unhashed) and **`AndroidScheduleMode.inexactAllowWhileIdle`**, deliberately avoiding the `SCHEDULE_EXACT_ALARM` restricted permission that Play reviews for alarm clocks (a habit nudge a few minutes late is fine; a policy rejection is not). Tap handling is on the same seam (`taps`) because only the instance that posted a notification can hear it tapped, and the **cold-start launch tap is replayed to the first subscriber** (the app is necessarily still booting when it is observed, so a plain broadcast stream would drop exactly the tap that started it). `NotificationPermissionRequester` seam mirroring `CameraPermissionRequester`, but it **does** report its outcome (a grant is what flips reminders on). Planner copy moved into the ARB via an injected `NotificationStrings` (the service layer must not import `AppLocalizations`; `notificationStringsProvider` resolves the platform locale itself since there is no `BuildContext` outside the widget tree). New UI: onboarding step 5 (`OnboardingNotificationsScreen`, both footer buttons complete onboarding, "Allow" enables reminders **only if granted** so the app never claims it will remind you when it cannot), the Notifications settings screen (`lib/src/ui/settings/notifications_screen.dart`), and a new shared `CairnSwitch` (the design corpus had no switch before). `NotificationTapListener` wraps `AppShell` on `OnboardingGate`'s post-onboarding branch only, and routes through a new shared **`startProofFlow`** (`lib/src/ui/proof/proof_entry.dart`, extracted from Home's `_handleProveIt`) so the notification route cannot drift from Home's guard chain; a tap naming a past date or a since-deleted task is **dropped silently** rather than surfacing the untranslated back-fill safety net. Decisions worth keeping: `notificationSchedulerProvider` keys off the **host** platform (`dart:io`), not `defaultTargetPlatform` - `flutter test` reports android while running on the desktop VM with no platform channels, so this gives every widget test the inert scheduler for free instead of each one having to remember an override. Refactors that came with it: `AppSettingsOpener.openCameraSettings` -> `openAppSettings` (both call sites land on the same OS page; the old name promised a per-permission deep link the platform does not offer), Profile's private `_PanelSurface`/`_HairlineDivider` promoted to shared `SettingsPanel`/`HairlineDivider`, and the verification step's `_PointCard` to `OnboardingPointCard`. **Android build note:** `flutter_local_notifications` 10+ requires **core library desugaring**; `android/app/build.gradle.kts` gained `isCoreLibraryDesugaringEnabled = true`, `multiDexEnabled = true`, and `coreLibraryDesugaring("com.android.tools:desugar_jdk_libs:2.1.4")` (pinned to what that plugin's README specifies, so it must be bumped alongside the plugin). Without it the build fails outright at `checkDebugAarMetadata`, even for an app that schedules nothing.
  - **Device pass ✅ DONE (2026-08-06, phone `GMRO9X6DUSJJHUD6`, Android 11).** Verified on hardware: the 5-step onboarding and its new Reminders screen; "Allow notifications" enabling reminders (with **no OS prompt on Android 11**, which is correct - `POST_NOTIFICATION` only became a runtime permission on 13); the Notifications settings screen including the dim-when-master-off behaviour and the time picker persisting; **14 alarms registered in Android's own alarm manager**, Aug 6-19 inclusive, confirming `rollingWindowDays` on real hardware, with a clean cancel-and-reschedule to the new time after a settings change; delivery at **+27s and +93s** past the scheduled instant (inside the inexact window, exactly the accepted `SCHEDULE_EXACT_ALARM` trade); the ARB copy interpolating correctly ("Time to prove \"Read 20 pages\"."); **tap-to-camera on BOTH warm and cold start** landing on the right occurrence; both channels visible in Android's per-app settings; and the **blocked state** (user-confirmed: the clay notice appears with the switches still showing their real saved values rather than snapping off, and clears on resume once re-granted). Method notes for future passes: use `am kill`, NOT `am force-stop`, to test cold start - force-stop puts the app in Android's "stopped" state and **cancels all its alarms**, silently invalidating the test. And do not poll `dumpsys notification | grep pkg=com.thnorty.cairn` to detect delivery: a `post_frequency{...}` stats line matches that forever once the app has posted once; match `NotificationRecord.*pkg=` instead.
  - **Two real bugs the device pass caught, both fixed 2026-08-06.** (1) The notification **small icon rendered as a solid white square**: it pointed at `@mipmap/ic_launcher`, but Android rebuilds a small icon as a silhouette from its ALPHA channel, and the launcher icon is opaque edge to edge, so there was no shape to extract. Fixed by adding `android/app/src/main/res/drawable/ic_stat_cairn.xml` (the cairn on a transparent background, scaled 1.15 since a small icon gets no adaptive-icon mask and so needs no safe-zone inset) and pointing `AndroidInitializationSettings` at it. (2) The **`cairn_streak_warnings` channel did not exist on the device**: `flutter_local_notifications` creates a channel lazily when its first notification is posted, so the OS-level control that the in-app "Streak warnings" switch implies would not appear until a streak warning actually fired - weeks away for a new user, and a promise this code's own comments made. Fixed by creating **both** channels eagerly in `_initialize()` via `createNotificationChannel`, with the id/name/description for each defined once and shared by both the create and post paths so they cannot drift.
  - **Second device pass ✅ DONE (2026-08-06, Galaxy S23 `R5CT52B47DA`, Android 14 / API 34).** This device covers what the Android 11 Xiaomi structurally could not. Verified: the **real `POST_NOTIFICATIONS` runtime prompt** fires from onboarding step 5; **denying it completes onboarding but leaves reminders OFF** (the app never claims it will remind you when it cannot); turning the master switch on while denied **re-fires the prompt** (Android allows a second ask); denying again reaches the **blocked state**, where the clay "Android is blocking these" notice appears and **the switches keep showing their real saved values instead of snapping off**; "Open system settings" opens Android's `InstalledAppDetails` for Cairn; and granting there then returning **clears the notice on resume** and un-dims the panel. Also confirmed on this device: the **Android 12+ platform splash** (`values-v31`) renders with the correct background and an unclipped cairn - first hardware confirmation of the 288dp/192dp mask sizing - and the system bars blend into the parchment rather than showing the black status-bar strip the pre-31 path has on the Xiaomi. The "Delete account" row is correctly absent for an anonymous session.
  ⚠️ **Not device-verified:** a **streak-at-risk warning actually being delivered** (it needs a live streak plus an unproven day plus the 20:00 hour, not reachable in one sitting). Its planning logic is unit-tested and its channel is now created eagerly, so only the delivery of that one kind is unobserved.

### Standing constraints (all phases)

- The schema is **sync-ready from day one**: client-generated UUID v7 PKs, nullable `user_id`, `updated_at` epoch-millis for last-write-wins, `deleted_at` tombstones (never hard-delete).
- A Supabase MCP server is connected for **inspecting** the remote project. **Migrations go through the Supabase CLI** (`npx supabase migration new` / `db push`) - MCP cannot manage migration files. Do not create remote tables until Phase 4.
- Anything marked ⚠️ cannot be completed by an agent alone. Stop and hand back rather than stubbing it out.

## Stack

- Flutter; **Riverpod** for state; **drift** (SQLite) for the local DB.
- **rrule** package for weekly and monthly-nth-weekday recurrence *only*. Monthly day-of-month is hand-rolled (see below).
- `uuid` for UUID v7 IDs; `timezone` package where an explicit zone is needed (tests).

## Domain rules (do not violate)

- **Local calendar dates everywhere.** `occurrence_date`, streaks, and day boundaries use the user's *local* calendar date (day flips at local midnight). Never UTC dates for these. The `LocalDate` value type in `lib/src/models/local_date.dart` and the `Clock` abstraction (`lib/src/clock.dart`) exist for this - always get "today" from a `Clock`, never `DateTime.now()` directly in domain logic.
- **An occurrence is `(task, localDate, slot)`.** `due_times` is a JSON list of `"HH:mm"` strings; its index is the slot. Empty list = one untimed slot 0.
- **Monthly `day_of_month` clamps, never skips.** `day = min(month_day, lastDayOfMonth)`. A task on the 31st fires Feb 28 (29 in leap years). Standard RRULE `BYMONTHDAY` skips short months, which is why this generator is hand-rolled and must never be replaced with rrule.
- **Monthly `nth_weekday`** uses rrule `FREQ=MONTHLY;BYDAY=3FR` style; `month_nth = -1` means "Last" (`BYDAY=-1FR`).
- **No back-filling.** Completions may only be created for occurrences whose `occurrence_date == today` (local). Enforced in the repository layer.
- **One proof per slot per day:** `UNIQUE(task_id, occurrence_date, slot)` on completions.
- **Streaks are derived, never stored.** Computed on read by walking scheduled dates backward from today. A date counts iff *every* slot that date is complete (all-or-nothing). Today-pending doesn't break the streak; only a fully-elapsed incomplete scheduled date does. Non-scheduled dates are skipped, not misses. No midnight jobs, no stored counters.
- **Metres (`points_awarded`)** are computed at completion-insert time: base 10 m; + per-task streak bonus (+1 m per consecutive day of that task's streak counting today, so a first-ever completion earns +1 m; capped +10 m); + perfect-day bonus (+15 m attached to the day's final scheduled occurrence across all tasks). Total altitude = `SUM(points_awarded)` over non-tombstoned **verified** completions only.
- **A pending completion earns no metres until it is verified** (decided 2026-07-14). Its `points_awarded` is still computed and stored at insert time (the rule above stands) and it still counts toward **streaks**, the **daily cap** and the **perfect-day bonus**, so an offline user never loses a streak for something outside their control and nobody can farm unlimited pending completions past the 5/day cap. It simply contributes no altitude until the verdict lands, at which point the metres it was awarded at insert time start counting, unrecomputed. This is what makes the guideline's promise that **rank never decreases** true: a proof that is later rejected can no longer claw back metres, because it never counted any. `pendingAltitude()` exposes the withheld total. The UI treatment is now canonical in `design/`: Home shows an "Awaiting verification" chip and `9 stones · 13 m when verified` (the stone is placed, the metres wait), Profile shows `+13 m awaiting verification` under the total, and **`Cairn Verify Pending.dc.html`** is the post-capture outcome screen when the verifier is unreachable ("Saved. We'll verify it soon.", "Streak safe / counts today", "13 m held / lands on verify"). Note the design vocabulary: a **stone** is one completion (it builds a task's cairn), **metres** are the cumulative score and rank.
- **Per-task cairns (the "Cairn N · M stones" vocabulary), decided 2026-07-16.** A task's stones (its non-tombstoned completions, verified or pending alike) stack into numbered **cairns**, each holding up to **10 stones** (`PointsService.cairnCapStones`, tunable). Stones fill the current cairn in completion order; when it reaches 10 the cairn **caps** (a finished monument, plus the bonus below) and the next stone begins the next cairn. When the day-level **streak breaks** (a fully-elapsed scheduled date left incomplete) the current cairn is sealed early as **broken** with fewer than 10 stones, and the next completion starts a fresh one. The cairn a task is currently filling is **"Growing now"**; the very first is the **trailhead**. So **"Cairn N · M stones" means the task's Nth cairn with M stones placed in it**: N counts the cairns built so far plus the one in progress, M is the stones in that current cairn (0..9). It is NOT the task's creation order, nor its lifetime stone total. Home shows each task's current growing cairn; the Trail (`Cairn Trail.dc.html`) shows the selected task's whole ordered history of cairns (capped / broken / growing / trailhead). Cairn grouping is **derived on read** from completions plus scheduled dates, never stored (same discipline as streaks). Every "Cairn N · M stones" the app shows (Home's Today cards, the proof-outcome screens, the Trail's own hero) resolves through the one shared rule `CairnGrouping.currentCairn` (unified 2026-07-16): for a brand-new task it is Cairn 1 with 0 stones, right after a cap it is the just-capped cairn (not the empty next one), otherwise the in-progress or most-recent cairn. `TaskRepository.cairnNumbers` is now only a stable task-ordering key, never the displayed label.
- **Capping a cairn awards a bonus** (decided 2026-07-16): the stone that fills a cairn to its 10th earns an extra **+25 m** (`PointsService.cairnCapBonus`, tunable) on top of base + streak + perfect-day, computed and stored in that completion's `points_awarded` at insert time like every other bonus and bound by the same rules: withheld while pending, counted only once verified, never recomputed. Because the bonus lives on one specific stone, a rejection that tombstones that stone takes the bonus with it (the cairn falls back below 10 and no other stone inherits it). This is the same accepted "computed at insert, never recomputed" drift the streak bonus already carries, and it keeps rank monotonic.
- **Rank tiers** (cumulative metres): Pebble 0 · Cairn 150 · Ridge 450 · Crag 1,100 · Bluff 2,400 · Peak 5,000 · Summit 8,849.
- In Phase 1 completions are inserted via a debug action with `verification_status = 'verified'`.

## Layout

- `lib/src/db/` - drift tables + `AppDatabase` (codegen via `dart run build_runner build`)
- `lib/src/models/` - `LocalDate`, `Occurrence`, enums
- `lib/src/services/` - occurrence generator, streaks, points/rank
- `lib/src/repo/` - task + completion repositories (all writes go through here)
- `lib/src/debug/` - Phase 1 debug screen
- `test/` - unit tests are the acceptance criteria; run `flutter test` after every change.

## AI contributor workflow

- Read AGENTS.md in full before starting, then read every code and test file the task touches.
- Inspect `git status` before editing and preserve all unrelated or pre-existing changes.
- Work in small, self-contained, reviewable steps.
- Use any capable tool directly or through delegation, while keeping ownership and verification clear.
- After changing drift tables, run `dart run build_runner build --delete-conflicting-outputs`.
- Write tests for every behavior the task calls out. Do not weaken, skip, or delete existing tests to make a change pass.
- The final owner must independently inspect the real `git diff` and run `flutter analyze` plus `flutter test`. Do not rely on delegated or self-reported results.
- Stop at genuine human-only boundaries such as secrets, remote deployment, dashboard configuration, billing setup, or physical-device verification. Do not stub, fake, or work around them.
- Report exact verification results, deviations from the specification, and any remaining human gates.
- For work involving credentials, privileged APIs, or deletion, pass the detailed work order by file reference instead of embedding security-sensitive text in an inline command.

## Git

- **Commit and push upon completing important milestones and verified steps.** (Decided 2026-08-24).
- Use conventional-commit style (`feat:`, `fix:`, `test:`, `chore:`), a concise subject line, and a short body listing what changed and why.
- Ensure all tests pass (`flutter analyze` and `flutter test`) before committing and pushing.

## Character restriction

Do not use U+2014. Use another fitting character.
